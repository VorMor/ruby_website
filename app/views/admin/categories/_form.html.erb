<%= render "shared/errors", record: category %>

<%= form_with model: [ :admin, category ], class: "admin-form" do |form| %>
  <label>
    <span>Название</span>
    <%= form.text_field :name, required: true %>
  </label>

  <label>
    <span>Описание</span>
    <%= form.text_area :description, rows: 4 %>
  </label>

  <label>
    <span>Цвет</span>
    <%= form.color_field :color %>
  </label>

  <%= form.submit "Сохранить", class: "primary-button" %>
<% end %>